<%= render :partial => "vial_info" %>

<%= render :partial => "vial_maintenance" %>

<div id="move_to_trash">
	<%= image_tag 'bin_empty.png' %>
	<%= link_to "Move to Trash",
            { :action => :destroy_vial, :id => @vial.id },
            :confirm => "Are you sure you want to move this vial to the trash?"
		%>
</div>

<%= render :partial => "two_way_table" %>

<%= render :partial => "parents_info" %>