<!DOCTYPE html 
     PUBLIC "-//W3C//DTD XHTML 1.0 Strict//EN"
     "DTD/xhtml1-strict.dtd">
<html xmlns="http://www.w3.org/1999/xhtml" lang="en" xml:lang="en">

<head>
  <meta http-equiv="Content-Type" content="text/html; charset=iso-8859-1"/>
  <%= stylesheet_link_tag 'style' %>
  <%= javascript_include_tag :defaults %>
  
  <title>YAGS</title>
</head>

<body>
	
	<% logged_in = (session[:current_user_id] ? true : false) -%>
	<% current_user = User.find(session[:current_user_id]) if logged_in -%>
	<!-- it would be nice to have this come in from somewhere else -->

  <div class="bar">
      <div id="session-info">
          <% if logged_in -%>
            Logged in as: <%= current_user.username -%><br />
            <%= link_to "Logout", :controller => 'users', :action => "logout" %>
          <% else %>
            <%= link_to "Login", :controller => 'users', :action => "login" %>
          <% end %> 
      </div>
      <img id="logo" src="/images/Beelzebub_icon.png" alt="temp logo" />
      <h1>YAGS</h1>
  </div>
  
  <div class="main">
    <%= yield :layout %>
  </div>
  
  <div class="bar">
      <a href="/">Home page</a>
	  <% if logged_in %>
	    <% if current_user.group.name == 'student' %>
      		<%= link_to "Bench", :controller => 'bench', :action => "index" %>
		<% elsif current_user.group.name == 'instructor' %>
      		<%= link_to "Lab", :controller => 'lab', :action => "index" %>
		<% elsif current_user.group.name == 'admin' %>
      		<%= link_to "Users", :controller => 'users', :action => "index" %>
		<% end %>
	  <% end -%>
	  
	  <p style=" font-size: 11px; text-align: center">&copy; 2007 Calvin College | All Rights Reserved <br />
	  Other fine print information	  </p> 
  </div>
  
</body>
</html>

